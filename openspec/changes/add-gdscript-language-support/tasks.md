## 1. Contract And Tests

- [x] 1.1 Define GDScript language routing, parser behavior, and limitations
- [x] 1.2 Add failing parser, routing, verification, and codegen tests

## 2. Implementation

- [x] 2.1 Implement GDScript parser and parser aliases
- [x] 2.2 Route `.gd` through sync, verification, extraction, and code generation
- [x] 2.3 Exclude conventional GDScript test files from production sync

## 3. Verification And Distribution

- [x] 3.1 Run focused and repository-wide Go tests and builds
- [x] 3.2 Validate OpenSpec and touched-scope GDC structure
- [x] 3.3 Prove a clean GDScript `init -> sync -> check` CLI smoke
- [x] 3.4 Rebuild and verify repository-local and installed Windows binaries
